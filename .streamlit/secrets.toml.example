# UkrMediaNLP — Streamlit secrets (optional)
#
# Stable default hides RoBERTa / emotions. To enable them (needs ~2 GB free RAM):
#
# ALLOW_HEAVY_NLP = "1"
#
# Or force light mode explicitly:
#
# LIGHT_CLOUD = "1"
#
# No API keys are required. RSS feeds are public.
