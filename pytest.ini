[pytest]
testpaths = tests
python_files = test_*.py
markers =
    slow: downloads transformer models (optional integration test)
    postgres: requires TEST_DATABASE_URL (Postgres service)
addopts = -m "not slow"
